# Rename this file to ".zshrc.local" and write local settings

## pyenv
# export PYENV_ROOT="${HOME}/.pyenv"
# if [ -d "${PYENV_ROOT}" ]; then
#   export PATH=${PYENV_ROOT}/bin:$PATH
#   eval "$(pyenv init -)"
# fi
